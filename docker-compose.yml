services:
  web:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: django_app
    ports:
      - "8000:8000"
    env_file:
      - .env
    environment:
      - PYTHONUNBUFFERED=1
    # Caso use SQLite local (ENVIRONMENT=local no arquivo .env) e queira persistir os dados no host do Windows
    # volumes:
    #   - sqlite_data:/app_db_data
    #   # NOTA: Se utilizar volumes no SQLite, defina o caminho no .env para apontar para /app_db_data/db.sqlite3
    
    # Se quiser utilizar o banco PostgreSQL (Serviço abaixo), descomente as linhas a seguir:
    # depends_on:
    #   - db

  # Banco de dados PostgreSQL opcional para desenvolvimento ou homologação local
  # db:
  #   image: postgres:17-alpine
  #   container_name: django_db
  #   restart: always
  #   environment:
  #     - POSTGRES_DB=django_db
  #     - POSTGRES_USER=django_user
  #     - POSTGRES_PASSWORD=django_password
  #   ports:
  #     - "5432:5432"
  #   volumes:
  #     - postgres_data:/var/lib/postgresql/data

# volumes:
#   postgres_data:
